/* Window event the landing uses to open the floating Concierge (Octavia)
   from elsewhere on the page — e.g. the FAQ's "Ask Octavia" button. */
export const OPEN_CONCIERGE_EVENT = 'marketing:open-concierge';
